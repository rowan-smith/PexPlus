# PermissionsExPlus platform modules

This Maven parent contains the platform-neutral `Core` module and the `Spigot` and `Paper` plugin adapters. Build a platform adapter to obtain a server-loadable plugin JAR.

See the repository [README](../README.md) for server setup and configuration.
