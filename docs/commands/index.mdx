---
sidebar_position: 1
---

# Commands

import CommandList from '@site/src/components/CommandList';

Search or browse every `/pex` command. All commands require `permissions.command-framework` in contrast to `classic` which uses legacy PEX syntax. See **General commands — command framework** for more details.

<CommandList />
