# Default Groups

Default groups are automatically assigned to new players who don't have any groups.

## Setting the Default Group

You can set the default group in `config.yml`:

```yaml
default-group: guest
```

## Assigning Multiple Defaults

While the `config.yml` only supports one primary default, you can use contextual permissions or specific logic in the API to handle multiple entry-level groups.
