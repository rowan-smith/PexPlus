/**
 * Backend inspection and data-transfer handles for administrative plugins.
 *
 * <p>These types operate on configured backends without switching the active runtime backend
 * unless explicitly applied.</p>
 */
package dev.rono.permissions.api.backend;
